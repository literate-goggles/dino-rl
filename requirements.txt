# Blackwell GPUs (B200 = sm_100) need torch >= 2.7 built for CUDA 12.8.
# Install the cu128 wheel explicitly (see Dockerfile); the default index may
# ship a build without sm_100 kernels.
torch>=2.7.0
numpy>=2.4.4
scipy>=1.17.1
scikit-learn>=1.8.0
opencv-python>=4.13.0
matplotlib>=3.9.0
tensorboard>=2.20.0
selenium>=4.41.0
